import 'package:flutter/material.dart';

class buildPopupdialogue extends StatelessWidget {
  const buildPopupdialogue({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return AlertDialog(
      title: Text('Alert Dialogue'),
    );
  }
}
